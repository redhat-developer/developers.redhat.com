{{ attach_library('classy/node') }}

{# Validate our data prior to accessing it. #}
{% set num_courses = katacoda_course_info.courses|length %}

{% if num_courses is not empty and num_courses > 1 %}
  {% set subtitle = num_courses ~ ' lessons | ' ~ katacoda_course_info.course_time ~ ' minutes' %}
{% else %}
  {% set subtitle = num_courses ~ ' lesson | ' ~ katacoda_course_info.course_time ~ ' minutes' %}
{% endif %}

{% include '@components/card.twig' with {
  'attributes': attributes,
  'tag': {
    'class': 'fa-terminal',
    'text': 'Course'
  },
  'title': {
    'text': node.label,
  },
  'subtitle': subtitle,
  'body': content.body[0]['#text']|striptags,
  'cta': {
    'url': url,
    'text': 'Start'
  }
} only %}
