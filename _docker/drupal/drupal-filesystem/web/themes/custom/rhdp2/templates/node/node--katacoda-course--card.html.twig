{{ attach_library('classy/node') }}

{% include '@components/card.twig' with {
  'attributes': attributes,
  'tag': {
    'class': 'fa-terminal',
    'text': 'Course'
  },
  'title': {
    'text': node.label,
  },
  'subtitle': katacoda_course_info.subtitle,
  'body': content.body[0]['#text']|striptags,
  'cta': {
    'url': url,
    'text': 'Start'
  }
} only %}
