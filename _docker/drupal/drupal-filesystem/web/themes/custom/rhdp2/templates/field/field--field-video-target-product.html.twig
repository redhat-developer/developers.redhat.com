{%
  set classes = [
    'field',
    'field--name-' ~ field_name|clean_class,
    'field--type-' ~ field_type|clean_class,
    'field--label-' ~ label_display,
  ]
%}

{% include '@components/card-grid.twig' with {
    'attributes': attributes,
    'card_grid__wrapper_attributes': create_attribute({'class': ['for-logos']}),
    'content': items
  } only %}
