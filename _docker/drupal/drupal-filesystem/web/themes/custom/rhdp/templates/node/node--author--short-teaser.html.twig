<article{{ attributes.addClass('author short-teaser') }}>

  {{ content.field_headshot }}
  {{ title_prefix }}
  {% if not page %}
    <h3{{ title_attributes.addClass('author-name') }}>
      {{ label }}
    </h3>
  {% endif %}
  {{ title_suffix }}

  <div{{ content_attributes }}>
    {{ content|without('field_headshot') }}
  </div>

</article>
