import {Base} from "../Base.page";

export class GeneralErrorPage extends Base {
    constructor() {
        super({
            path: '/general-error/',
            pageTitle: 'Page Does Not Exist | Red Hat Developer'
        });
    }
}
